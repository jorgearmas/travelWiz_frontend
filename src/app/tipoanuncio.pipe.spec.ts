import { TipoanuncioPipe } from './tipoanuncio.pipe';

describe('TipoanuncioPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoanuncioPipe();
    expect(pipe).toBeTruthy();
  });
});
