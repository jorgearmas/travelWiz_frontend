import { LugaresOrigenPipe } from './lugares-origen.pipe';

describe('LugaresOrigenPipe', () => {
  it('create an instance', () => {
    const pipe = new LugaresOrigenPipe();
    expect(pipe).toBeTruthy();
  });
});
